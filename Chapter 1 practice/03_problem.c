#include  <stdio.h>

int main() {
    int r = 6;
    int h = 4;
    printf("the area of circle with radius %d is %f", r , 3.14 * r * r);
    printf("\nthe volume of cylinder with radius %d and height %d is %f", r, h, 3.14 * r * r * h);
    return 0;
}